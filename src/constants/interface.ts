export interface IDropdown{
  value: string | number;
  label: string;
}
export interface PreviewProps {
  allowEdit: boolean
}
export interface ICheckBoxDescription{
  id: number;
  name: string;
  description: string;
}